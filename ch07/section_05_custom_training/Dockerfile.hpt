# Inject a currently supported Agent Platform Python training base image.
ARG BASE_IMAGE
FROM ${BASE_IMAGE}

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY task.py hpt_task.py ./
ENTRYPOINT ["python", "hpt_task.py"]
